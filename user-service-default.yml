server:
  port: 8001

spring:
  profiles: default
  datasource:
    url: jdbc:mysql://localhost:3306/ntu_ci6225
    username: root
    password: root
    useSSL: false
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
      show-sql: true
      useSSL: false
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
      dialect: org.hibernate.dialect.MySQL5Dialect
